### Testing task 1:

# Carry out static testing on the code below.
# Comment on any errors that you see below.
```ruby
### Testing task 2 code:

# Carry out dynamic testing on the code below.
# Correct the errors below that you spotted in task 1.

require_relative('card.rb')
class CardGame

#add the attr and def initialize to CardGame class

  def checkforAce(card)
    if card.value = 1 #should be 'if card = 1'
      return true
    else
      return false
    end
  end

  # dif should be def and should be (card1, card2)
  dif highest_card(card1 card2)
  if card1.value > card2.value
    #dropped .value
    return card.name #changed to card1
  else
    card2
  end
end
end
# end in wrong place

def self.cards_total(cards)
  total# =0 to set the initial count
  for card in cards
    total += card.value
    return "You have a total of" + total
  end
end



```
